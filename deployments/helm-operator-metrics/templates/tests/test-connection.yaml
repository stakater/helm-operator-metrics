apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "helm-operator-metrics.fullname" . }}-test-connection"
  labels:
{{ include "helm-operator-metrics.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "helm-operator-metrics.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
